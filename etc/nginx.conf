upstream backend {:
    server 127.0.0.1:8000;
}

server {
    listen 80;
    location ^~ /uploads/ {
        root    /home/box/web/;
    }
    location ~* ^.+\.\w+$ {
        root    /home/box/web/public;
    }
    location / {
        proxy_pass http://backend;
    }
}
